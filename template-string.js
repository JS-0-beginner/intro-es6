// node template-string.js \\

const multipleLine = `Hi! I'm Avishek.
I'm from BD.
One of the worse country in Asia.`;

// console.log(multipleLine);

const surname = 'Molla'
const add = `Jasim ${surname} is a bad person`;

// console.log(add);

const first = 'Avishek';
const last = 'Ataur';

const FullName = `${first} ${last}`;
const singleLine = `Hi I'm 
${first} ${last}`;

// console.log(FullName);
console.log(singleLine);